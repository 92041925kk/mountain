# 中原大學登山社網站｜後台操作手冊

> 這份手冊是寫給**接手網站的幹部**看的，不需要任何程式背景。
> 日常更新（改文字、換照片、發布行程）都可以在後台完成，**改完馬上生效，不需要請工程師部署**。

---

## 目錄

1. [最重要的觀念：改內容 vs 改程式](#1-最重要的觀念改內容-vs-改程式)
2. [怎麼登入後台](#2-怎麼登入後台)
3. [後台有哪些功能](#3-後台有哪些功能)
4. [常見操作步驟](#4-常見操作步驟)
5. [每學期開學要做的事](#5-每學期開學要做的事)
6. [怎麼新增 / 移除管理員帳號](#6-怎麼新增--移除管理員帳號)
7. [遇到問題怎麼辦](#7-遇到問題怎麼辦)

---

## 1. 最重要的觀念：改內容 vs 改程式

網站的更新分成兩種，**請先搞清楚你要做的是哪一種**：

| 你想做的事 | 怎麼做 | 需要工程師嗎？ |
|---|---|---|
| 改首頁文字、標語、介紹 | **後台 → 網站設定** | ❌ 不用，存檔即生效 |
| 換首頁封面照片 | **後台 → 照片管理 / 網站設定** | ❌ 不用 |
| 新增 / 修改出隊行程紀錄 | **後台 → 行程紀錄管理** | ❌ 不用 |
| 更新學期行事曆 | **後台 → 行事曆管理** | ❌ 不用 |
| 修改 FAQ 常見問題 | **後台 → 網站設定** | ❌ 不用 |
| 上傳 / 刪除照片 | **後台 → 照片管理** | ❌ 不用 |
| 改網站的**排版、顏色、版面結構、新功能** | 需要改程式碼 | ✅ 要會操作的人 |

**結論：絕大多數的日常維護都在後台就能完成，不用碰程式、不用部署。**
只有要動到網站「長相」或「功能」時，才需要找會寫程式的人（見[第 7 點](#7-遇到問題怎麼辦)）。

---

## 2. 怎麼登入後台

1. 在網址列輸入：`你的網站網址/cymc-admin`
   （例如 `https://cymc2-7d93e.web.app/cymc-admin`）
2. 輸入**管理員 Email 和密碼**後按「登入」。
3. 登入後會進到**後台首頁**，上面有目前學期、行程數等資訊，以及各管理功能的入口。

> 沒有帳號嗎？請見[第 6 點](#6-怎麼新增--移除管理員帳號)。

---

## 3. 後台有哪些功能

登入後，後台首頁有四個管理區塊：

| 區塊 | 用途 |
|---|---|
| 🗓️ **行事曆管理** | 編輯本學期出隊行程（顯示在「學期行事曆」頁與首頁「即將出發」） |
| 🖼️ **照片管理** | 上傳新照片、刪除照片、設定首頁照片牆要顯示哪些 |
| 🥾 **行程紀錄管理** | 撰寫每次出隊的詳細時間線（顯示在「隊伍回顧」頁） |
| ⚙️ **網站設定** | 改首頁文字、封面、FAQ、目前學期等全站設定 |

---

## 4. 常見操作步驟

### 4-1. 改首頁文字 / 標語 / 介紹

1. 後台首頁 → **網站設定**
2. 找到要改的欄位（首頁標題、副標、介紹區文字…）直接修改
3. 拉到最下面按「**儲存設定**」
4. 回到網站首頁重新整理就會看到變化

### 4-2. 換首頁封面照片

1. 先到 **照片管理** 上傳你要的封面照片
2. 上傳後，把照片的網址或 Storage 路徑，填到 **網站設定 → 首頁封面 Storage 路徑**
3. 按「儲存設定」

### 4-3. 上傳照片到照片牆

1. 後台首頁 → **照片管理**
2. 把圖片**拖進上傳區**，或按「選擇圖片」（可一次選多張）
3. （選填）填說明文字、綁定行程、是否「首頁精選」
4. 按「**上傳**」，系統會自動壓縮後上傳
5. 勾選「首頁精選」的照片會出現在首頁照片牆

### 4-4. 更新學期行事曆

1. 後台首頁 → **行事曆管理**
2. 輸入學期（例如 `114-2`）按「載入」；若是新學期會出現「建立新學期」
3. 按「**＋ 新增行程**」，填寫：
   - **日期**：格式要寫成 `5/1` 或 `5/1-5/3`（用斜線和減號，⚠️ 不要寫「5月1日」否則首頁抓不到）
   - **行程名稱**、**FB 連結**（沒有就填「無」）、**簡介**（選填）
4. 改完按「**儲存全部**」

### 4-5. 新增一筆出隊行程紀錄

1. 後台首頁 → **行程紀錄管理** → 按「**＋ 新增行程**」
2. 填基本資料：行程名稱、學期、天數、地區、難度
3. 行程計畫可以**手動一筆筆新增**，也可以**上傳「活動記錄紙 PDF」自動轉換**（系統會幫你產生時間線，套用前可調整）
4. 確認沒問題後：
   - 還沒寫完 → 按「**建立草稿**」（不會顯示在前台，下次可繼續編輯）
   - 確定要公開 → 按「**發布**」
5. 系統有**自動暫存功能**：編輯到一半關掉，下次打開會問要不要還原

---

## 5. 每學期開學要做的事

每學期初記得做這三件事，網站才會顯示新學期的內容：

1. **網站設定 → 目前學期**：改成新學期（例如從 `114-1` 改成 `114-2`），按儲存
2. **行事曆管理**：建立新學期、把這學期的出隊行程填進去
3. 確認首頁「即將出發」有正確顯示近期的隊伍

---

## 6. 怎麼新增 / 移除管理員帳號

管理員帳號是用 **Firebase**（Google 提供的後端服務）管理的，不是在網站後台。

1. 到 [Firebase Console](https://console.firebase.google.com/)，用**有權限的 Google 帳號**登入
2. 選擇本社團的專案（`cymc2-7d93e`）
3. 左側選單 → **Authentication（驗證）** → **Users（使用者）**
4. 按「**Add user（新增使用者）**」，輸入新幹部的 Email 和一組密碼
5. 把帳密交給對方，請他第一次登入後自行記住

> **移除卸任幹部**：在同一個 Users 列表找到該帳號，刪除即可。
> **交接提醒**：請把「誰有 Firebase Console 權限」記錄在社團交接文件裡，避免畢業後沒人進得去。

---

## 7. 遇到問題怎麼辦

- **改了後台但網站沒變？** 先把網站頁面**重新整理**（Ctrl + F5）。後台資料是即時的，通常重整就會看到。
- **首頁「即將出發」沒顯示隊伍？** 檢查行事曆的**日期格式**是不是寫成 `5/1` 這種（不能寫「5月1日」），而且日期要是**今天之後**的。
- **要改網站的版面、顏色、功能？** 這屬於「改程式」，需要會操作的人。請找：
  - 上一屆負責網站的學長姐
  - 或把這份 repo 交給有 Nuxt / Vue 經驗的社員
  - 工程相關說明在 [README.md](README.md)
- **照片上傳失敗？** 確認圖片小於 15MB、格式是常見的 jpg / png。

---

*最後更新：請接手者在大幅修改後更新此日期，並補上自己看到的常見問題。*
